jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: npm install
  test:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: npm test
workflows:
  version: 2
  pr-check:
    jobs:
      - build
      - test
